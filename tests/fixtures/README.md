# Test fixtures

Known-good and known-bad layout JSON files for unit and integration tests.

Fixtures are added in plans 02–03 (piece catalogue and connection engine).

- `pieces.json` — MVP piece port coordinates and transformed geometry snapshots
  (headings 0, 1, 4) used as regression oracles by `tests/unit/piece-catalogue/`.
